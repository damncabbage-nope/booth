#= require spec_helper

describe "Browsing Events", ->

  it "displays a list of active Events"
  it "displays a list of upcoming events"
  it "does not display events that have finished"
  it "displays the details of an event when clicked"
